#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int ch1;

	for (ch1 = 0; ch1 < 10; ch1++)
	{
		printf("%d", ch1);
	}
	putchar(10);

	return (0);
}
